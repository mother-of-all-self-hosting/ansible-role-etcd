{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>mother-of-all-self-hosting/renovate-config#v1.1.0"
  ],
  "packageRules": [
    {
      "description": "Minor and patch bumps automerge - wider than the shared //patch-automerge preset, hence a local rule. The `migration` Molecule scenario starts the pinned version over a store written by the old image, so a bump is gated as an upgrade rather than a fresh install. Majors wait for a person.",
      "matchManagers": [
        "custom.regex"
      ],
      "matchDepNames": [
        "gcr.io/etcd-development/etcd"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": true,
      "automergeType": "branch"
    },
    {
      "description": "Majors always get a pull request. Last, so it overrides the rule above.",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    }
  ]
}
